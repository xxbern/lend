openapi: 3.0.2
info:
  version: 1.0.0
  title: Lend
  description: 借用记录对象

components:
  schemas:
    Lend:
      type: object
      description: 借用记录
      properties:
        id:
          type: integer
          format: int32
          description: 借用id
